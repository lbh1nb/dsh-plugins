/**
 * dsh-steer-button (host half). No host capability is needed: the client
 * steers through the session binding's prompt(mode: 'steer'). This module
 * exists so the profile bundle row has a host face.
 * @module dsh-steer-button
 */
export const name = 'dsh-steer-button'
export const inject = []
export function apply() {}
